import React from 'react';

const FilterBar = (props) => {
    return (
        <div style={{ height: '30px', border: '2px dashed black' }}>
            Filter Bar
        </div>
    );
};

export default FilterBar;
